# Gate Q probe report

Generated by GitHub Actions before raw-signal Gate Q.

## `sub-p17_task-cl_ieeg.vmrk`

Total markers: **0**

| type | description | count |
|---|---|---:|

## `sub-p17_task-clcontrol_ieeg.vmrk`

Total markers: **0**

| type | description | count |
|---|---|---:|

## `sub-p19_task-cl_ieeg.vmrk`

Total markers: **0**

| type | description | count |
|---|---|---:|

## `sub-p19_task-clcontrol_ieeg.vmrk`

Total markers: **0**

| type | description | count |
|---|---|---:|

# Authors-code search

## `TSS-main/README.md`

Matched: `stim`

```text
7: If you are interested in using our approach for closed-loop stimulation, see the [[Toolkit for Oscillatory Real-Time Tracking and Estimation](https://github.com/tne-lab/TORTE)] which depends upon [[Open Ephys GUI](https://github.com/open-ep
```

## `TSS-main/tss/analysis/run_ged_phaselocked.m`

Matched: `0.05`

```text
18: % Here, it selects timepoints between -0.050 and 0.800 seconds relative to each event.
```

## `TSS-main/tss/analysis/run_group_rest_coherence.m`

Matched: `stim`

```text
4: % during resting state, both pre- and post-stimulation, and performs statistical analysis.
30:         stim_info = get_stim_info(subjects{s});
32:             is_stim = strcmp(dat.contacts, stim_info.anode) | ...
33:                 strcmp(dat.contacts, stim_info.cathode);
35:             is_stim = strcmp(dat.contacts, 'D''10') | ...
45:                                                 is_stim, ...
49:         % compute phase locking and stats -- stim channel to entire brain
50:         [pre_conn{s}, post_conn{s}] = get_conn(freq_pre, freq_post, is_stim);
59:         stim_chans = freq_pre.label(is_stim);
66:         stim_info = get_stim_info(subjects{s});
68:             is_stim = strcmp(dat.contacts, stim_info.anode) | ...
69:                 strcmp(dat.contacts, stim_info.cathode);
71:             is_stim = strcmp(dat.contacts, 'D''10') | ...
82:                                                 is_stim);
84:         [con_pre_conn{s}, con_post_conn{s}] = get_conn(freq_pre, freq_post, is_stim);
92:         stim_chans = freq_pre.label(is_stim);
194: function [pre_conn, post_conn] = get_conn(freq_pre, freq_post, is_stim)
198: pairs = pairs(any(ismember(pairs, find(is_stim)),2), :);
304:     is_stim, is_phase)
311:     is_stim, ...
319:     is_stim, ...
```

## `TSS-main/tss/analysis/pred_stim_phase.m`

Matched: `stim`, `Stimulus`

```text
1: function [est_phase, pred_dat] = pred_stim_phase(dat, hilb_window)
3: %PRED_STIM_PHASE Predicts the phase of a stimulus signal.
5: %   [est_phase, pred_dat] = PRED_STIM_PHASE(dat, hilb_window) takes the input data
7: %   the stimulus signal. The function returns the estimated phase 'est_phase'
15: %       est_phase - A vector containing the estimated phase for each trial.
19: %       [est_phase, pred_dat] = pred_stim_phase(dat, 50);
```

## `TSS-main/tss/analysis/compare_p2p.m`

Matched: `stim`

```text
4: %  timelock_pre - timelock structure for the pre-stimulation period
5: %  timelock_post - timelock structure for the post-stimulation period
```

## `TSS-main/tss/analysis/run_conn_lme.m`

Matched: `marker`, `stim`, `0.05`

```text
30: stim_con = ones(size(all_y));
31: stim_con = cat(1, stim_con, 2*ones(size(con_y)));
44:     conn_table.C = categorical(stim_con(:,freq_idx));
67:                any(stim_con==1,2);
74:                any(stim_con==1,2);
83:                any(stim_con==2,2);
90:                any(stim_con==2,2);
100: %   pb_pre_y - phase-blind pre-stimulation connectivity data
101: %   pb_post_y - phase-blind post-stimulation connectivity data
102: %   cl_pre_y - closed-loop pre-stimulation connectivity data
103: %   cl_post_y - closed-loop post-stimulation connectivity data
124:     hp(s).MarkerFaceColor = 'r'; %#ok<AGROW,AGROW>
129:          'XLim', [0.95 10.05], ...
149:     hp(s).MarkerFaceColor = 'r';
153: ylabel({'Time x Stim', 'Interaction (\beta)'})
157:          'XLim', [0.95 10.05], ...
```

## `TSS-main/tss/analysis/run_ep_stats_phasechan.m`

Matched: `stim`, `0.05`

```text
3: %RUN_EP_STATS_PHASECHAN compares evoked potential before and after stimulation
6: %   timelock_pre - FieldTrip timelock structure for pre-stimulation data
7: %   timelock_post - FieldTrip timelock structure for post-stimulation data
8: %   phase_chan - channel used for phase-locking stimulation
29: cfg.alpha            = 0.05; % .05 per tail to gen. cluster
```

## `TSS-main/tss/analysis/run_group_cl_phase.m`

Matched: `stim`

```text
5: % It reads and preprocesses the data, computes phase estimates, and generates various metrics.
86:         [est_phase{s}, pred_dat{s}] = pred_stim_phase(dat, 80);
98:     % if this subj has control stim
101:         [control_est_phase{s}, control_pred_dat{s}] = pred_stim_phase(dat_control, 80);
250:                                 'VariableNames', {'SubjectID','Stim Condition','Phase Consistency','Iteration'});
264:                                 'VariableNames', {'SubjectID','Stim Condition','Phase Consistency', 'Iteration'});
```

## `TSS-main/tss/analysis/run_group_ep.m`

Matched: `stim`

```text
10: %           timelock_pre - Preprocessed data for the pre-stimulation period.
11: %           timelock_post - Preprocessed data for the post-stimulation period.
12: %           con_timelock_pre - Preprocessed control data for the pre-stimulation period.
13: %           con_timelock_post - Preprocessed control data for the post-stimulation period.
49:     if strcmp(subjects{s}, 'p16') % Subject p16 has inverted pre-stimulation epochs due to incorrect recording settings.
54:     if strcmp(subjects{s}, 'p19') % Subject p19 has inverted pre- and post-stimulation epochs due to incorrect recording settings.
59:     stim_info = get_stim_info(subjects{s});
60:     phase_chan = {stim_info.phase};
```

## `TSS-main/tss/analysis/run_group_ged.m`

Matched: `stim`, `0.25`

```text
248: stim = get_stim_info(subject);
250:     is_stim = strcmp(clean_dat.label, stim.anode) | ...
251:         strcmp(clean_dat.label, stim.cathode);
253:     is_stim = strcmp(clean_dat.label, 'D''10') | ...
273: [p2p_late, max_t_late, min_t_late] = compute_p2p(res, [.05 0.25]);
415: % stim and phase lock info
416: stim = get_stim_info(subjid);
420:     if isequal(contacts{c}, stim.phase)
```

## `TSS-main/tss/analysis/run_conn_stats.m`

Matched: `0.05`

```text
32: cfg.alpha            = 0.05; % .05 per tail to gen. cluster
```

## `TSS-main/tss/analysis/run_group_bosc.m`

Matched: `stim`

```text
37:     stim_info = get_stim_info(subjects{subjIdx});
39:     if isequal(chantype,'stim')
41:             phase_chan = {stim_info.anode stim_info.cathode};
```

## `TSS-main/tss/preprocessing/run_ep_preproc.m`

Matched: `stim`

```text
3: %RUN_EP_PREPROC runs preprocessing for stimulation evoked potential data
6: %   pre_epoched_dat: pre-stimulation epoched data
7: %   post_epoched_dat: post-stimulation epoched data
17: %   timelock_pre: timelock data for pre-stimulation data
18: %   timelock_post: timelock data for post-stimulation data
19: %   trial_idx_pre: trial indices for pre-stimulation data
20: %   trial_idx_post: trial indices for post-stimulation data
```

## `TSS-main/tss/preprocessing/run_rest_preproc.m`

Matched: `stim`, `0.25`

```text
2:     fs, contacts, chansel, is_stim, is_phase)
10: %   is_stim: whether channel is a stimulation channel
33: cfg.trials = all(k(is_stim | is_phase,:) < kthr); %mean(k<kthr) > .95; %all(k < kthr);
59: cfg.foi       = 1.5:0.25:50;
```

## `TSS-main/tss/preprocessing/run_cl_ep_preproc.m`

Matched: `stim`

```text
3: %RUN_CL_EP_PREPROC runs preprocessing for closed-loop stimulation evoked
```

## `TSS-main/tss/plotting/plot_theta_effects.m`

Matched: `marker`, `stim`

```text
2: % PLOT_THETA_EFFECTS plots the effects of theta power on stimulation
48: hp(1).MarkerFaceColor = color;
49: hp(1).MarkerEdgeColor = 'none';
54: hp(1).MarkerFaceAlpha = .5;
65: xlabel('Stimulation Pulse (% of Total)');
95: hp(2).MarkerFaceColor = color;
96: hp(2).MarkerEdgeColor = 'none';
101: hp(2).MarkerFaceAlpha = .5;
111: xlabel('Stimulation Pulse (% of Total)');
```

## `TSS-main/tss/plotting/plot_example_subj.m`

Matched: `0.25`

```text
47: p2p_late = compute_p2p(res, [.05 0.25]);
```

## `TSS-main/tss/plotting/plot_group_rose.m`

Matched: `marker`

```text
36: hlp.LineWidth = 1; hlp.Color = 'k'; hlp.MarkerFaceColor = 'r'; hlp.MarkerSize = 4;
61: hlp.LineWidth = 1; hlp.Color = 'k'; hlp.MarkerFaceColor = [0 0 1]; hlp.MarkerSize = 4;
```

## `TSS-main/tss/plotting/plot_group_conn.m`

Matched: `stim`

```text
7: %   pb_pre_y: pre-stimulation phase-blind connectivity data
8: %   pb_post_y: post-stimulation phase-blind connectivity data
9: %   cl_pre_y: pre-stimulation closed-loop connectivity data
10: %   cl_post_y: post-stimulation closed-loop connectivity data
```

## `TSS-main/tss/plotting/plot_group_amp_quantiles_swarm.m`

Matched: `marker`

```text
55: hl.Marker = 'o';
56: hl.MarkerFaceColor = 'r';
57: hl.MarkerSize = 4;
66: hl.Marker = 'o';
67: hl.MarkerSize = 4;
68: hl.MarkerFaceColor = 'b';
99: he.Marker = 'o';
100: he.MarkerFaceColor = 'r';
101: he.MarkerSize = 4;
107: hec.Marker = 'o';
108: hec.MarkerFaceColor = 'b';
109: hec.MarkerSize = 4;
```

## `TSS-main/tss/plotting/plot_subj_cl_performance.m`

Matched: `stim`

```text
3: % the group for closed-loop stimulation
64:     [est_phase, pred_dat] = pred_stim_phase(dat, 80);
76: % if this subj has control stim
79:     [control_est_phase, control_pred_dat] = pred_stim_phase(dat_control, 80);
```

## `TSS-main/tss/plotting/plot_interaction_lme.m`

Matched: `stim`

```text
47: ylabel('Time by Stim Interaction (\beta)');
```

## `TSS-main/tss/plotting/group_p2p_plot.m`

Matched: `marker`

```text
31:     h(i).MarkerFaceColor = 'flat';
32:     h(i).MarkerFaceAlpha = .5;
33:     h(i).MarkerEdgeAlpha = .5;
```

## `TSS-main/tss/plotting/plot_group_quantiles_swarm.m`

Matched: `marker`, `stim`

```text
58: hl.Marker = 'o';
59: hl.MarkerFaceColor = 'r';
67: hl.Marker = 'o';
68: hl.MarkerFaceColor = 'b';
92: he.Marker = 'o';
93: he.MarkerFaceColor = 'r';
101: hec.Marker = 'o';
102: hec.MarkerFaceColor = 'b';
196: xlabel('Stimulation Type');
```

## `TSS-main/tss/plotting/plot_p2p_example.m`

Matched: `stim`

```text
101: % GET_P2P_QUANTILES computes quantile estimation for peak-to-peak amplitude data
```

## `TSS-main/tss/utils/get_theta_quantiles.m`

Matched: `stim`

```text
2: % GET_THETA_QUANTILES computes quantile estimation for theta power data.
5: %   n_bin: number of bins for quantile estimation
```

## `TSS-main/tss/utils/get_amp_quantiles.m`

Matched: `stim`

```text
2: % GET_AMP_QUANTILES computes quantile estimation for peak-to-peak
6: %   n_bin: number of bins for quantile estimation
28: % GET_QUANTILE_SUMMARY computes quantile estimation for a given data.
33: %   n_bin: number of bins for quantile estimation
```

## `TSS-main/tss/utils/get_stim_info.m`

Matched: `stim`

```text
1: function stim = get_stim_info(subjid)
2: %GET_STIM_INFO returns stimulation info for a given subject
8: %   stim: structure containing stimulation info
14:         stim.anode = 'B10'; % -
15:         stim.cathode = 'B11'; %+ 
16:         stim.phase = 'B1-B2';
18:         stim.anode = 'D10'; % -
19:         stim.cathode = 'D11'; % +
20:         stim.phase = 'D1-D2';
22:         stim.anode = 'P7'; % -
23:         stim.cathode = 'P8'; % +
24:         stim.phase = 'P2';
26:         stim.anode = 'RB8'; % -
27:         stim.cathode = 'RB9'; % +
28:         stim.phase = 'RB1';
30:         stim.anode = 'D7'; % -
31:         stim.cathode = 'D8'; % +
32:         stim.phase = 'D1';
34:         stim.anode = 'AH6'; % -
35:         stim.cathode = 'AH7'; % +
36:         stim.phase = 'AH2';
38:         stim.anode = 'D9';
39:         stim.cathode = 'D10';
40:         stim.phase = 'D1';
42:         stim.anode = {'D9','C1','C''1','D''10'}; % -
43:         stim.cathode = {'D10','C2','C''2','D''11'}; % +
44:         stim.phase = 'C''1'; % control only
46:         stim.anode = 'RHT6';
47:         stim.cathode = 'RHT7';
48:         stim.phase = 'RHH1'; %control only
50:         stim.anode = 'RHB7';
51:         stim.cathode = 'RHB8';
52:         stim.phase = 'RHB1'; %control only
```

## `TSS-main/tss/utils/get_subj_eeg.m`

Matched: `stim`

```text
29: % find the contacts that we want to include, based on stimulation
32: stim = get_stim_info(subjid);
35: chan_include = startsWith(contacts, stim.anode(isletter(stim.anode))) | ...
36:     startsWith(contacts, stim.cathode(isletter(stim.cathode))) | ... 
37:     startsWith(contacts, stim.phase(isletter(stim.phase))) | ...
40:     labels = {stim.anode{:} stim.cathode{:}};
81: % durations to epoch around closed-loop stim
```

## `TSS-main/tss/utils/get_phase_chan.m`

Matched: `stim`

```text
12: % stim and phase lock info
13: stim = get_stim_info(subjid);
15: if contains(stim.phase,'-')
16:     stim.phase = strsplit(stim.phase,'-');
21:     if any(strcmp(contacts{c}, stim.phase))
```

## `TSS-main/tss/utils/get_cl_control_phase_chan.m`

Matched: `stim`

```text
12: % stim and phase lock info
13: stim = get_stim_info(subjid);
15: if contains(stim.cl_control_phase,'-')
16:     stim.cl_control_phase = strsplit(stim.cl_control_phase,'-');
21:     if any(strcmp(contacts{c}, stim.cl_control_phase))
```

## `TSS-main/tss/utils/get_group_synchrony.m`

Matched: `stim`

```text
6: %   pre_conn: pre-stimulation connectivity data
7: %   post_conn: post-stimulation connectivity data
31:     stim_info = get_stim_info(subjects{s});
33:     is_phase = endsWith(pre_conn{s}.label, stim_info.phase) | ...
34:                startsWith(pre_conn{s}.label, [stim_info.phase '-']);
37:     tmp = mean(pre_conn{s}.(measure)(is_phase,:,:), 'omitnan'); % across both stim contacts
48:     tmp = mean(post_conn{s}.(measure)(is_phase,:,:), 'omitnan'); % across both stim contacts
```

## `TSS-main/tss/utils/get_condition_info.m`

Matched: `stim`

```text
10: %               and closed-loop stim
26:         % timestamps for closed-loop stim
48:         % timestamps for closed-loop stim
63:         % timestamps for closed-loop stim
77:         % timestamps for closed-loop stim
100:         % timestamps for closed-loop stim
115:         % timestamps for closed-loop stim
136:         % timestamps for closed-loop stim
156:         % timestamps for closed-loop stim
170:         %timestamps for stim
184:         %timestamps for stim
```
